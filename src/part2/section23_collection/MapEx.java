package part2.section23_collection;

/*
    Map 컬렉션
        - Map 컬렉션 '키' - '값' 으로 구성된 엔트리 객체를 저장한다.
        - '키' 는 중복으로 사용할 수 없다.
        - 만약 동일한 '키' 에 새로운 '값'을 덮어쓴다.
    Map 컬렉션 메소드
        - 객체 추가: put()
        - 객체 검색: containsKey(), containsValue(), entrySet(), get(키), isEmpty(), keySet(), size(), values()
        - 객체 삭제: clear(), remove(키)
 */
public class MapEx {
}
