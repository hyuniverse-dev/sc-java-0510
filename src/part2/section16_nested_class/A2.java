package part2.section16_nested_class;

/*
    정적 멤버 클래스
        - static 키워드와 함께 A 클래스의 멤버로 선언된 중첩 클래스이다.
        - 정적 멤버 클래스로 정의하면 B 객체는 A 클래스 내/외부 어디서든지 사용이 가능하다.
 */
public class A2 {

    // 정적 멤버 클래스
    static class B {
        
    }
}
