package part2.section23_collention;

/*
    Set 컬렉션
        - 중복을 허용하지 않고, 순서를 보장하지 않는다.
        - 구슬주머니에 비유한다. 동일한 구슬을 두 개 넣을 수 없고, 저장할 때와 나올 때 순서가 다를 수 있기 때문이다.
    Set 컬렉션 종류
        - HashSet
        - LinkedHashSet
        - TreeSet
    Set 컬렉션 메소드
        - 객체 추가: add()
        - 객체 검색: contains(), isEmpty(), iterator(), size()
        - 객체 삭제: clear(), remove(인덱스X -> 객체 기반으로 삭제)
    Set 특징
        - 인덱스가 존재하지 않기 때문에 인덱스로 관리하는 메소드가 없다.
 */
public class SetEx {
}
